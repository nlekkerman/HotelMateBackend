# Breakfast Room Service - Complete Flow Diagrams

## 🎯 System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         GUEST DEVICE                            │
│                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    │
│  │  QR Scanner  │───▶│  React App   │◀──▶│   Pusher     │    │
│  └──────────────┘    └──────────────┘    │   Client     │    │
│                             │              └──────────────┘    │
│                             │              ┌──────────────┐    │
│                             └─────────────▶│   Firebase   │    │
│                                            │   (FCM)      │    │
└─────────────────────────────────────────────└──────────────┘────┘
                                                      │
                                                      │ HTTPS
                                                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                       BACKEND SERVER                            │
│                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    │
│  │    Django    │───▶│  PostgreSQL  │    │   Cloudinary │    │
│  │  REST API    │    │   Database   │    │  (QR Images) │    │
│  └──────────────┘    └──────────────┘    └──────────────┘    │
│         │                                                       │
│         │            ┌──────────────┐    ┌──────────────┐    │
│         └───────────▶│    Pusher    │    │   Firebase   │    │
│                      │    Server    │    │    Admin     │    │
│                      └──────────────┘    └──────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Real-time / Push
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      STAFF DEVICES                              │
│                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    │
│  │   Kitchen    │    │    Porter    │    │    Waiter    │    │
│  │     App      │    │     App      │    │     App      │    │
│  └──────────────┘    └──────────────┘    └──────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Guest Order Flow (Step-by-Step)

```
┌──────────────────────────────────────────────────────────────────┐
│                    GUEST JOURNEY                                 │
└──────────────────────────────────────────────────────────────────┘

Step 1: QR Code Scanning
┌─────────────────┐
│  Guest scans    │
│  QR code in     │──▶ Extract hotel_slug + room_number
│  room           │
└─────────────────┘
         │
         ▼
Step 2: Load Menu
┌─────────────────┐
│  GET /breakfast │──▶ Backend returns items by category
│  Display items  │
└─────────────────┘
         │
         ▼
Step 3: Browse & Add to Cart
┌─────────────────┐
│  Guest selects  │
│  items          │──▶ Local cart state updated
│  + quantities   │
└─────────────────┘
         │
         ▼
Step 4: Proceed to Checkout
┌─────────────────┐
│  Click "Order"  │──▶ Show PIN validation screen
└─────────────────┘
         │
         ▼
Step 5: PIN Validation
┌─────────────────┐
│  Enter 4-char   │
│  PIN            │──▶ POST /validate-pin/
└─────────────────┘    │
         │              │
    ┌────▼────┐        │
    │ Valid?  │        │
    └────┬────┘        │
         │             │
    YES  │        NO   │
         │             └──▶ Show error, retry
         ▼
Step 6: Request Push Notifications
┌─────────────────┐
│  Request FCM    │
│  permission     │──▶ POST /save-fcm-token/
└─────────────────┘
         │
         ▼
Step 7: Select Delivery Time
┌─────────────────┐
│  Choose time    │
│  slot           │──▶ 7:00-8:00, 8:00-8:30, etc.
└─────────────────┘
         │
         ▼
Step 8: Submit Order
┌─────────────────┐
│  POST /orders   │──▶ Backend creates order
│  + items        │    │
│  + time         │    │
└─────────────────┘    │
         │              ▼
         │         ┌──────────────┐
         │         │  Order saved │
         │         │  to database │
         │         └──────────────┘
         │              │
         ▼              ▼
Step 9: Order Confirmation
┌─────────────────┐    ┌──────────────────┐
│  Show success   │    │  Notify staff:   │
│  Order #42      │    │  - Kitchen       │
│  Status: pending│    │  - Porters       │
└─────────────────┘    │  - Waiters       │
         │              └──────────────────┘
         │
         ▼
Step 10: Real-Time Tracking
┌─────────────────┐
│  Subscribe to   │◀──▶ Pusher channel
│  Pusher channel │    {hotel}-room-{room_number}
└─────────────────┘
         │
         │ Receive updates
         ▼
┌─────────────────┐
│  Status changes:│
│  pending →      │
│  accepted →     │
│  completed      │
└─────────────────┘
```

---

## 👨‍💼 Staff Order Management Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                    STAFF WORKFLOW                                │
└──────────────────────────────────────────────────────────────────┘

Step 1: Staff Login
┌─────────────────┐
│  Staff logs in  │──▶ Get auth token
│  to app         │
└─────────────────┘
         │
         ▼
Step 2: Subscribe to Updates
┌─────────────────┐
│  Connect to     │
│  Pusher channel │──▶ {hotel}-staff-{staff_id}-{role}
└─────────────────┘
         │
         ▼
Step 3: Load Pending Orders
┌─────────────────┐
│  GET /orders    │──▶ Fetch all pending/accepted orders
│  with filters   │
└─────────────────┘
         │
         ▼
Step 4: Display Orders
┌─────────────────────────────────────┐
│  Order List:                        │
│  ┌─────────────────────────────┐   │
│  │ Order #42                   │   │
│  │ Room: 101                   │   │
│  │ Time: 8:00-8:30             │   │
│  │ Status: PENDING             │   │
│  │ Items: Eggs x2, Juice x1    │   │
│  │ [Accept] [View Details]     │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
         │
         ▼
Step 5: Process Order

    Kitchen Staff:
    ┌─────────────────┐
    │  Click "Accept" │──▶ PATCH /orders/{id}/
    │  status: accepted│   {"status": "accepted"}
    └─────────────────┘
             │
             ▼
    ┌─────────────────┐
    │  Prepare food   │
    └─────────────────┘
             │
             ▼
    ┌─────────────────┐
    │  Click "Complete"│──▶ PATCH /orders/{id}/
    │  status: completed│  {"status": "completed"}
    └─────────────────┘

    Porter:
    ┌─────────────────┐
    │  Receive        │◀── FCM/Pusher notification
    │  notification   │
    └─────────────────┘
             │
             ▼
    ┌─────────────────┐
    │  Pick up food   │
    │  Deliver to     │
    │  Room 101       │
    └─────────────────┘

         │
         ▼
Step 6: Guest Notification
┌─────────────────┐
│  Guest receives │
│  status update  │──▶ Via Pusher (live) + FCM (push)
│  on device      │
└─────────────────┘
```

---

## 🔔 Notification Flow

```
┌──────────────────────────────────────────────────────────────────┐
│              WHEN ORDER IS CREATED                               │
└──────────────────────────────────────────────────────────────────┘

Order Created (POST /breakfast-orders/)
         │
         ├──────────────────────────────────────┐
         │                                      │
         ▼                                      ▼
Backend saves to DB                  Backend triggers signals
         │                                      │
         │                                      ├─────┐
         │                                      │     │
         ▼                                      ▼     ▼
Return order to guest             Pusher        FCM Push
                                 Real-time      Notifications
         │                          │               │
         │                          ├───────┬───────┴──────┐
         │                          │       │              │
         ▼                          ▼       ▼              ▼
Guest sees confirmation      Kitchen    Porters      Waiters
                             Staff      (on duty)   (on duty)
                             (browser    (mobile     (browser
                              open)       device)     open)


┌──────────────────────────────────────────────────────────────────┐
│            WHEN STATUS CHANGES (STAFF UPDATE)                    │
└──────────────────────────────────────────────────────────────────┘

Staff updates status (PATCH /orders/{id}/)
         │
         ├──────────────────────────────────────┐
         │                                      │
         ▼                                      ▼
Backend updates DB                   Backend sends notifications
         │                                      │
         │                                      ├─────┐
         │                                      │     │
         ▼                                      ▼     ▼
Return updated order              Pusher        FCM Push
to staff                         Guest Ch.      to Guest
         │                          │               │
         │                          │               │
         ▼                          ▼               ▼
Staff sees update            Guest browser    Guest mobile
                             (if open)        (always)
```

---

## 🔐 Security & Validation Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                  PIN VALIDATION PROCESS                          │
└──────────────────────────────────────────────────────────────────┘

Guest enters PIN
       │
       ▼
POST /validate-pin/
{"pin": "abc1"}
       │
       ▼
Backend checks Room.guest_id_pin
       │
       ├──────────────────┐
       │                  │
       ▼                  ▼
   Match?              No Match?
   ✓ Valid             ✗ Invalid
       │                  │
       ▼                  ▼
Return {valid: true}  Return {valid: false}
       │                  │
       ▼                  ▼
Allow order           Deny & show error
       │
       ▼
Proceed to checkout


┌──────────────────────────────────────────────────────────────────┐
│              STATUS TRANSITION VALIDATION                        │
└──────────────────────────────────────────────────────────────────┘

PATCH /orders/{id}/ {"status": "NEW_STATUS"}
       │
       ▼
Backend validates transition
       │
       ├─────────────────────────────┐
       │                             │
       ▼                             ▼
   Valid transition?            Invalid?
   (pending→accepted)           (pending→completed)
   (accepted→completed)
       │                             │
       ▼                             ▼
   Update status                Return 400 error
   Save to DB                   {"error": "Invalid transition"}
       │
       ▼
   Notify all parties
```

---

## 📱 Data Synchronization

```
┌──────────────────────────────────────────────────────────────────┐
│            MULTI-DEVICE SYNCHRONIZATION                          │
└──────────────────────────────────────────────────────────────────┘

Scenario: Guest has 2 devices, Staff member updates order

Device A (Browser)          Backend              Device B (Mobile)
       │                       │                        │
       │◀──── Pusher ──────────┤                        │
       │      Channel          │                        │
       │   (Real-time)         │                        │
       │                       │                        │
       │                       │◀───── FCM ────────────▶│
       │                       │     (Push)             │
       │                       │                        │
       ▼                       ▼                        ▼
   UI updates             Database             Notification
   instantly              consistent           appears
   "Accepted"                                  "Order accepted"


┌──────────────────────────────────────────────────────────────────┐
│              OFFLINE HANDLING                                    │
└──────────────────────────────────────────────────────────────────┘

Device goes offline
       │
       ▼
Local state preserved
(cart, selected items)
       │
       ▼
Device comes back online
       │
       ├──────────────────┐
       │                  │
       ▼                  ▼
Pusher reconnects    Fetch latest data
automatically        GET /orders/
       │                  │
       └────────┬─────────┘
                ▼
         Sync complete
         UI up-to-date
```

---

## 🎯 Complete System Interaction

```
┌────────────────────────────────────────────────────────────────────┐
│                  FULL SYSTEM FLOW                                  │
└────────────────────────────────────────────────────────────────────┘

Time: 7:45 AM
┌─────────────┐
│   Guest     │  Scans breakfast QR code in room
└──────┬──────┘
       │ GET /breakfast-menu
       ▼
┌─────────────┐
│   Backend   │  Returns items: Eggs, Toast, Juice, Coffee
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Guest     │  Selects: Eggs x2, Coffee x1
└──────┬──────┘  Enters PIN: "abc1"
       │ POST /validate-pin
       ▼
┌─────────────┐
│   Backend   │  PIN valid ✓
└──────┬──────┘  Saves FCM token
       │
       ▼
┌─────────────┐
│   Guest     │  Selects delivery: 8:00-8:30
└──────┬──────┘  Clicks "Order"
       │ POST /breakfast-orders
       ▼
┌─────────────┐
│   Backend   │  Creates Order #42
└──────┬──────┘  Status: pending
       │
       ├─────────────────────┬─────────────────────┐
       │                     │                     │
       ▼                     ▼                     ▼
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Kitchen   │       │   Porter    │       │   Guest     │
│    Staff    │       │             │       │             │
└──────┬──────┘       └──────┬──────┘       └──────┬──────┘
       │ Pusher+FCM          │ Pusher+FCM          │
       │ notification        │ notification        │ Confirmation
       ▼                     ▼                     ▼
  "New order!"          "Delivery needed"     "Order placed!"

Time: 7:47 AM
┌─────────────┐
│   Kitchen   │  Clicks "Accept"
│    Staff    │
└──────┬──────┘
       │ PATCH status: "accepted"
       ▼
┌─────────────┐
│   Backend   │  Updates Order #42
└──────┬──────┘  Status: accepted
       │
       ├─────────────────────┬─────────────────────┐
       │                     │                     │
       ▼                     ▼                     ▼
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Kitchen   │       │   Porter    │       │   Guest     │
└──────┬──────┘       └──────┬──────┘       └──────┬──────┘
       │                     │ Update              │ Pusher+FCM
       ▼                     ▼                     ▼
  Starts cooking      Prepare to deliver    "Being prepared"

Time: 8:05 AM
┌─────────────┐
│   Kitchen   │  Food ready
│    Staff    │  Clicks "Complete"
└──────┬──────┘
       │ PATCH status: "completed"
       ▼
┌─────────────┐
│   Backend   │  Updates Order #42
└──────┬──────┘  Status: completed
       │
       ├─────────────────────┐
       │                     │
       ▼                     ▼
┌─────────────┐       ┌─────────────┐
│   Porter    │       │   Guest     │
└──────┬──────┘       └──────┬──────┘
       │ Notification        │ Pusher+FCM
       ▼                     ▼
  Picks up food         "On the way!"

Time: 8:10 AM
┌─────────────┐
│   Porter    │  Delivers to Room 101
└─────────────┘  Guest enjoys breakfast! 🍳☕
```

---

## 🔄 Error Handling Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                  ERROR SCENARIOS                                 │
└──────────────────────────────────────────────────────────────────┘

Scenario 1: Invalid PIN
┌─────────────┐
│ Guest enters│
│ wrong PIN   │──▶ POST /validate-pin {"pin": "wrong"}
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Backend    │──▶ Returns {"valid": false}
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Guest     │──▶ Shows error message
│             │──▶ Clear input, allow retry
└─────────────┘


Scenario 2: Item Out of Stock
┌─────────────┐
│ Guest adds  │
│ item to cart│──▶ Check is_on_stock field
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Frontend   │──▶ If false, disable "Add" button
│             │──▶ Show "Out of Stock" badge
└─────────────┘


Scenario 3: Invalid Status Transition
┌─────────────┐
│ Staff tries │
│ pending →   │──▶ PATCH {"status": "completed"}
│ completed   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Backend    │──▶ Validates transition
│             │──▶ Returns 400 error
└──────┬──────┘    {"error": "Invalid transition"}
       │
       ▼
┌─────────────┐
│ Staff UI    │──▶ Shows error alert
│             │──▶ Maintains current status
└─────────────┘


Scenario 4: Network Failure During Order
┌─────────────┐
│ Guest clicks│
│ "Place Order"│──▶ POST /breakfast-orders
└──────┬──────┘
       │
       ▼ Network timeout
┌─────────────┐
│  Frontend   │──▶ Catch error
│             │──▶ Show retry option
│             │──▶ Keep cart intact
└─────────────┘
       │
       ▼ Guest clicks "Retry"
┌─────────────┐
│  Resend     │──▶ POST /breakfast-orders
│  request    │    (with same data)
└─────────────┘
```

---

## 📊 Database State Flow

```
┌──────────────────────────────────────────────────────────────────┐
│              DATABASE TABLES INTERACTION                         │
└──────────────────────────────────────────────────────────────────┘

    BreakfastItem         BreakfastOrder      BreakfastOrderItem
   ┌──────────────┐      ┌──────────────┐      ┌──────────────┐
   │ id           │      │ id           │      │ id           │
   │ hotel_id     │◀────┐│ hotel_id     │      │ order_id     │───┐
   │ name         │     ││ room_number  │◀────┐│ item_id      │◀──┤
   │ category     │     ││ status       │     ││ quantity     │   │
   │ is_on_stock  │     ││ delivery_time│     │└──────────────┘   │
   └──────────────┘     ││ created_at   │     │                   │
                        │└──────────────┘     │                   │
                        │                     │                   │
                        └─────────────────────┘                   │
                                                                  │
   Room                  Hotel                                    │
   ┌──────────────┐      ┌──────────────┐                        │
   │ id           │      │ id           │                        │
   │ hotel_id     │──────▶│ name         │                        │
   │ room_number  │      │ slug         │                        │
   │ guest_id_pin │      └──────────────┘                        │
   │ guest_fcm_token│                                             │
   └──────────────┘                                               │
                                                                  │
                                                                  │
Order Creation Flow:                                              │
1. Create BreakfastOrder (room_number, hotel, status, time)      │
2. For each cart item:                                            │
3.   Create BreakfastOrderItem (order, item, quantity) ──────────┘
```

---

**This completes the visual flow documentation! 🎉**

Use these diagrams to understand:
- System architecture
- User journeys  
- Staff workflows
- Real-time notifications
- Error handling
- Data synchronization
